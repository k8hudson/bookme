<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
  <fieldset class="form_group">
    <p><label>Comienza el</label><br />
    <%= f.datetime_select :starts_at %></p>
    <p><label>Termina el</label><br />
    <%= f.datetime_select :ends_at %></p>
    <p><%= f.check_box :all_day %>
    <label>Todo el día</label></p>
  </fieldset>

  <div class="actions">
    <%= f.submit 'Crear Evento', class: "btn btn-success" %>
  </div>
<% end %>
